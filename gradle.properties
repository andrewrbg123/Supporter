# Gradle itself runs on the JDK that launches it; the Java toolchain below is what the
# plugin is actually compiled against. The server runs JDK 25.
org.gradle.java.installations.auto-detect=true
org.gradle.caching=true
